{
  "name": "ezimage-obsidian",
  "version": "1.1.2",
  "description": "Obsidian plugin for EzImage - Upload images to cloud storage",
  "main": "main.js",
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production",
    "version": "node version-bump.mjs && git add manifest.json versions.json"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/node-fetch": "^2.6.11",
    "esbuild": "0.17.3",
    "obsidian": "latest",
    "tslib": "2.6.0",
    "typescript": "5.0.4"
  },
  "dependencies": {
    "browser-image-compression": "^2.0.2"
  }
}
